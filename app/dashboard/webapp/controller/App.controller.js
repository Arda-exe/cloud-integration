sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("primepath.dashboard.controller.App", {

        onTabSelect: function (oEvent) {
            // navigatie via router komt in stap 2 (routing)
        }

    });
});
