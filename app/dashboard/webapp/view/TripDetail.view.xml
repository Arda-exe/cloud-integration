<mvc:View
    controllerName="primepath.dashboard.controller.TripDetail"
    displayBlock="true"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:f="sap.f"
    xmlns:card="sap.f.cards"
    xmlns:core="sap.ui.core"
    xmlns="sap.m">

    <Page
        id="tripPage"
        title="{i18n>tripDetailTitle}"
        showNavButton="true"
        navButtonPress=".onNavBack"
        busy="{trip>/busy}"
        busyIndicatorDelay="200">

        <content>
            <ObjectHeader
                title="{trip>/trip/Name}"
                intro="{trip>/personName}"
                number="{trip>/trip/Budget}"
                numberUnit="USD"
                responsive="true">
                <attributes>
                    <ObjectAttribute title="{i18n>lblPeriod}" text="{trip>/periodText}" />
                </attributes>
            </ObjectHeader>

            <FlexBox wrap="Wrap" alignItems="Stretch" class="sapUiSmallMarginTop">
            <Panel headerText="{i18n>routeTitle}" class="sapUiSmallMarginBeginEnd" visible="{trip>/route/has}">
                <layoutData>
                    <FlexItemData growFactor="2" baseSize="42rem" />
                </layoutData>
                <content>
                    <VBox class="sapUiSmallMargin">
                        <HBox justifyContent="Center" alignItems="Center" class="sapUiSmallMarginBottom">
                            <VBox alignItems="Center">
                                <Title text="{trip>/route/fromIata}" level="H2" />
                                <Text text="{trip>/route/fromName}" textAlign="Center" />
                            </VBox>
                            <core:Icon src="sap-icon://flight" size="2rem" class="sapUiLargeMarginBegin sapUiLargeMarginEnd" />
                            <VBox alignItems="Center">
                                <Title text="{trip>/route/toIata}" level="H2" />
                                <Text text="{trip>/route/toName}" textAlign="Center" />
                            </VBox>
                        </HBox>

                        <FlexBox wrap="Wrap" justifyContent="Center">
                            <f:Card width="20rem" class="sapUiSmallMargin">
                                <f:header>
                                    <card:Header title="{trip>/route/fromName}" subtitle="{i18n>routeDeparture}" iconSrc="sap-icon://journey-depart" />
                                </f:header>
                                <f:content>
                                    <VBox class="sapUiSmallMargin">
                                        <HBox class="sapUiTinyMarginBottom">
                                            <Label text="{i18n>lblCity}" width="9rem" />
                                            <Text text="{trip>/route/fromCity}" />
                                        </HBox>
                                        <HBox class="sapUiTinyMarginBottom">
                                            <Label text="{i18n>lblCoordinates}" width="9rem" />
                                            <Text text="{trip>/route/fromCoords}" />
                                        </HBox>
                                        <HBox>
                                            <Label text="{i18n>lblTripsViaAirport}" width="9rem" />
                                            <Text text="{trip>/route/fromTrips}" />
                                        </HBox>
                                    </VBox>
                                </f:content>
                            </f:Card>

                            <f:Card width="20rem" class="sapUiSmallMargin">
                                <f:header>
                                    <card:Header title="{trip>/route/toName}" subtitle="{i18n>routeArrival}" iconSrc="sap-icon://journey-arrive" />
                                </f:header>
                                <f:content>
                                    <VBox class="sapUiSmallMargin">
                                        <HBox class="sapUiTinyMarginBottom">
                                            <Label text="{i18n>lblCity}" width="9rem" />
                                            <Text text="{trip>/route/toCity}" />
                                        </HBox>
                                        <HBox class="sapUiTinyMarginBottom">
                                            <Label text="{i18n>lblCoordinates}" width="9rem" />
                                            <Text text="{trip>/route/toCoords}" />
                                        </HBox>
                                        <HBox>
                                            <Label text="{i18n>lblTripsViaAirport}" width="9rem" />
                                            <Text text="{trip>/route/toTrips}" />
                                        </HBox>
                                    </VBox>
                                </f:content>
                            </f:Card>
                        </FlexBox>
                    </VBox>
                </content>
            </Panel>

            <Panel headerText="{i18n>coTravellersTitle}" class="sapUiSmallMarginBeginEnd"
                visible="{= ${trip>/coTravellers}.length > 0 }">
                <layoutData>
                    <FlexItemData growFactor="1" baseSize="20rem" />
                </layoutData>
                <content>
                    <List items="{trip>/coTravellers}">
                        <StandardListItem
                            title="{trip>name}"
                            description="{trip>userName}"
                            icon="sap-icon://employee"
                            iconDensityAware="false"
                            type="Navigation"
                            press=".onCoTravellerPress" />
                    </List>
                </content>
            </Panel>
            </FlexBox>

            <Panel headerText="{i18n>tripInfoTitle}" class="sapUiSmallMarginBeginEnd sapUiSmallMarginTop">
                <content>
                    <VBox class="sapUiSmallMargin">
                        <Text text="{trip>/trip/Description}" />
                    </VBox>
                </content>
            </Panel>

            <Panel headerText="{i18n>approvalTitle}" class="sapUiSmallMarginBeginEnd">
                <content>
                    <VBox class="sapUiSmallMargin">
                        <HBox alignItems="Center">
                            <Label text="{i18n>lblApprovalStatus}" width="7rem" />
                            <ObjectStatus text="{trip>/ext/statusText}" state="{trip>/ext/statusState}" />
                        </HBox>
                    </VBox>
                </content>
            </Panel>

            <Panel headerText="{i18n>flightsTitle}" class="sapUiSmallMarginBeginEnd sapUiSmallMarginBottom">
                <content>
                    <Table
                        id="flightsTable"
                        items="{trip>/flights}"
                        busy="{trip>/flightsBusy}"
                        busyIndicatorDelay="200"
                        noDataText="{i18n>flightsNoData}"
                        sticky="ColumnHeaders">

                        <columns>
                            <Column>
                                <Text text="{i18n>colFlight}" />
                            </Column>
                            <Column minScreenWidth="Tablet" demandPopin="true">
                                <Text text="{i18n>colFrom}" />
                            </Column>
                            <Column minScreenWidth="Tablet" demandPopin="true">
                                <Text text="{i18n>colTo}" />
                            </Column>
                            <Column minScreenWidth="Desktop" demandPopin="true">
                                <Text text="{i18n>colDeparture}" />
                            </Column>
                            <Column minScreenWidth="Desktop" demandPopin="true">
                                <Text text="{i18n>colArrival}" />
                            </Column>
                            <Column width="5rem" hAlign="End">
                                <Text text="{i18n>colSeat}" />
                            </Column>
                        </columns>

                        <items>
                            <ColumnListItem vAlign="Middle">
                                <cells>
                                    <ObjectIdentifier title="{trip>flightTitle}" text="{trip>airlineName}" />
                                    <Link text="{trip>fromName} ({trip>fromIata})" press=".onFromPress" />
                                    <Link text="{trip>toName} ({trip>toIata})" press=".onToPress" />
                                    <Text text="{trip>departure}" />
                                    <Text text="{trip>arrival}" />
                                    <Text text="{trip>seat}" />
                                </cells>
                            </ColumnListItem>
                        </items>

                    </Table>
                </content>
            </Panel>
        </content>

        <footer>
            <OverflowToolbar visible="{app>/user/isCoordinator}">
                <ToolbarSpacer />
                <Button
                    text="{i18n>btnSubmit}"
                    type="Emphasized"
                    enabled="{= !${trip>/ext/exists} }"
                    press=".onSubmitApproval" />
                <Button
                    text="{i18n>btnApprove}"
                    type="Accept"
                    enabled="{trip>/ext/exists}"
                    press=".onApprove" />
                <Button
                    text="{i18n>btnReject}"
                    type="Reject"
                    enabled="{trip>/ext/exists}"
                    press=".onReject" />
            </OverflowToolbar>
        </footer>

    </Page>

</mvc:View>
