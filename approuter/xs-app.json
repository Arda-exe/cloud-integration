{
    "welcomeFile": "/dashboard/webapp/index.html",
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^(.*)$",
            "destination": "srv-api",
            "authenticationType": "xsuaa"
        }
    ]
}